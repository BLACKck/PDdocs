# 宠物服务系统版本控制计划

## 1. 版本控制概述

本版本控制计划旨在规范宠物服务系统的代码管理，确保代码的安全性、可追溯性和协作效率。采用Git作为版本控制工具，制定明确的分支管理策略和提交规范。

## 2. 代码仓库结构

### 2.1 仓库组织

- **主仓库**：宠物服务系统主代码库
  - 包含前端和后端代码
  - 包含所有配置文件和文档

- **分支结构**：
  - `main`：主分支，用于发布稳定版本
  - `develop`：开发分支，集成所有功能开发
  - `feature/*`：功能分支，用于开发新功能
  - `bugfix/*`： bug修复分支，用于修复生产环境问题
  - `release/*`：发布分支，用于准备发布版本

### 2.2 目录结构

```
pet-service-system/
├── frontend/           # 对客前端代码
│   ├── src/            # 源代码
│   ├── public/         # 静态资源
│   ├── package.json    # 依赖配置
│   └── README.md       # 前端说明文档
├── backend/            # 店端管理系统后端代码
│   ├── src/            # 源代码
│   ├── config/         # 配置文件
│   ├── package.json    # 依赖配置
│   └── README.md       # 后端说明文档
├── docs/               # 项目文档
│   ├── 项目计划.md      # 项目计划
│   ├── 需求文档.md      # 需求文档
│   ├── 系统流程图.md    # 系统流程图
│   ├── 高保真原型设计.md # 高保真原型设计
│   ├── 质量检查报告.md   # 质量检查报告
│   └── 版本控制计划.md   # 版本控制计划
├── .gitignore          # Git忽略文件
└── README.md           # 项目说明文档
```

## 3. 分支管理策略

### 3.1 主分支

- **main分支**：
  - 仅用于发布稳定版本
  - 每次发布后打标签
  - 不直接接受代码提交，仅通过合并请求更新

- **develop分支**：
  - 集成所有功能开发
  - 作为开发的默认分支
  - 定期从main分支合并最新代码

### 3.2 辅助分支

- **feature分支**：
  - 从develop分支创建
  - 命名格式：`feature/功能名称`
  - 功能开发完成后合并回develop分支

- **bugfix分支**：
  - 从main分支创建
  - 命名格式：`bugfix/问题描述`
  - 修复完成后同时合并回main和develop分支

- **release分支**：
  - 从develop分支创建
  - 命名格式：`release/版本号`
  - 用于准备发布版本，进行最后的测试和修复
  - 发布完成后合并回main和develop分支

## 4. 提交规范

### 4.1 提交信息格式

```
<类型>(<范围>): <描述>

[可选的正文]

[可选的页脚]
```

### 4.2 提交类型

- `feat`：新功能
- `fix`：bug修复
- `docs`：文档更新
- `style`：代码风格调整
- `refactor`：代码重构
- `test`：测试代码
- `chore`：构建过程或辅助工具的变动

### 4.3 提交示例

```
feat(frontend): 添加宠物圈发帖功能

- 实现帖子发布界面
- 添加图片/视频上传功能
- 实现标签选择功能

Closes #123
```

## 5. 代码审查流程

### 5.1 合并请求流程

1. 开发者在功能分支上完成开发
2. 提交代码并推送至远程仓库
3. 创建合并请求，指定目标分支
4. 团队成员进行代码审查
5. 审查通过后，由项目负责人合并代码
6. 合并后删除功能分支

### 5.2 代码审查要点

- 代码质量：是否符合代码规范
- 功能实现：是否满足需求
- 安全性：是否存在安全隐患
- 性能：是否存在性能问题
- 测试：是否有相应的测试代码

## 6. 版本号规范

采用语义化版本号规范：`MAJOR.MINOR.PATCH`

- `MAJOR`：重大版本，不兼容的API变更
- `MINOR`：次版本，向下兼容的功能添加
- `PATCH`：补丁版本，向下兼容的问题修复

## 7. 发布流程

1. 从develop分支创建release分支
2. 在release分支上进行最后的测试和修复
3. 测试通过后，合并到main分支
4. 在main分支上打标签，标签格式：`vX.Y.Z`
5. 将main分支的变更合并回develop分支
6. 部署发布版本

## 8. 工具配置

### 8.1 Git配置

- 用户名：开发者真实姓名
- 邮箱：公司邮箱
- 提交签名：建议开启GPG签名

### 8.2 钩子脚本

- **pre-commit**：代码风格检查
- **pre-push**：运行测试
- **commit-msg**：提交信息格式检查

### 8.3 代码规范工具

- 前端：ESLint、Prettier
- 后端：ESLint、Prettier（Node.js）或CheckStyle（Java）

## 9. 协作规范

### 9.1 开发流程

1. 从develop分支创建功能分支
2. 定期从develop分支拉取最新代码
3. 提交代码前运行测试
4. 创建合并请求，等待代码审查
5. 代码审查通过后，合并到develop分支

### 9.2 冲突解决

- 遇到冲突时，先拉取最新代码
- 手动解决冲突
- 重新运行测试
- 提交解决冲突的代码

## 10. 备份与恢复

- 定期备份代码仓库
- 使用Git的分支和标签功能进行版本管理
- 遇到重大问题时，可回滚到之前的稳定版本

## 11. 工具推荐

- **代码托管**：GitHub、GitLab、Gitee
- **代码审查**：GitHub Pull Requests、GitLab Merge Requests
- **CI/CD**：GitHub Actions、GitLab CI、Jenkins
- **代码质量**：SonarQube、CodeClimate

## 12. 版本控制计划更新

本计划将根据项目进展和团队反馈进行定期更新，确保版本控制流程的有效性和适应性。