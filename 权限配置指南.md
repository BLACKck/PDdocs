# 飞书云文档 API 权限配置指南

## 问题描述
当前错误：`required one of these privileges under the user identity: [docx:document, docx:document:create]`

## 需要的权限

创建云文档需要以下权限：

### 1. 用户身份权限（必须）
- **权限名称**: `docx:document`
- **权限描述**: 访问和管理云文档
- **权限类型**: 用户身份

### 2. 创建权限（必须）
- **权限名称**: `docx:document:create`
- **权限描述**: 创建云文档
- **权限类型**: 用户身份

### 3. 读取权限（推荐）
- **权限名称**: `docx:document:read`
- **权限描述**: 读取云文档内容
- **权限类型**: 用户身份

## 当前已开通权限

根据截图显示：
- ✅ `docx:document:readonly` (用户身份) - 只读权限
- ✅ `docx:document:readonly` (应用身份) - 只读权限

**问题**: 缺少 `docx:document:create` 权限

## 解决方案

### 步骤 1：访问飞书开放平台
1. 访问：https://open.feishu.cn/
2. 登录开发者账号

### 步骤 2：进入应用管理
1. 点击"开发者后台"
2. 在应用列表中找到您的应用（俄罗斯方块，App ID: cli_a92fb9e90e799cb5）
3. 点击进入应用详情页

### 步骤 3：开通权限
1. 点击左侧菜单"权限管理"
2. 点击"开通权限"按钮
3. 搜索以下权限并开通：
   - `docx:document` (查看和编辑新版文档)
   - `docx:document:create` (创建新版文档)
   - `docx:document:read` (查看新版文档)

### 步骤 4：配置权限
1. 对于每个权限，选择"用户身份"
2. 配置可访问的数据范围（建议选择"与用户权限范围一致"）
3. 点击"保存"

### 步骤 5：提交审核
1. 权限开通后需要提交审核
2. 填写应用使用说明
3. 提交等待审核通过（通常 1-2 个工作日）

### 步骤 6：重新授权
1. 审核通过后，需要用户重新授权
2. 使用新的授权码获取 User Access Token
3. 或者使用 refresh_token 刷新 token

## 权限对比

| 权限名称 | 权限代码 | 用途 | 当前状态 |
|---------|---------|------|---------|
| 查看新版文档 | docx:document:readonly | 只读访问文档 | ✅ 已开通 |
| 编辑新版文档 | docx:document | 编辑现有文档 | ❌ 未开通 |
| 创建新版文档 | docx:document:create | 创建新文档 | ❌ 未开通 |
| 删除新版文档 | docx:document:delete | 删除文档 | 可选 |

## 快速开通链接

直接访问权限开通页面：
https://open.feishu.cn/console/permission

## 验证权限

开通权限后，运行以下命令验证：
```bash
python create_test_doc_final.py
```

如果权限配置正确，应该能看到：
```
✓ 云文档创建成功！
文档 URL: https://xxx.feishu.cn/docx/xxxxx
```

## 注意事项

1. **权限类型**: 必须选择"用户身份"，不是"应用身份"
2. **数据范围**: 建议选择"与用户权限范围一致"
3. **审核时间**: 权限开通后需要审核，审核通过后才能使用
4. **重新授权**: 权限变更后需要用户重新授权获取新的 User Access Token

## 常见问题

### Q: 为什么开通了权限还是报错？
A: 可能的原因：
- 权限类型选择错误（应选用户身份）
- 权限还未审核通过
- User Access Token 是旧授权的，需要重新获取

### Q: 如何确认权限是否生效？
A: 
1. 检查权限状态是否为"已开通"
2. 确认权限类型是"用户身份"
3. 重新获取 User Access Token
4. 运行测试脚本验证

## 联系支持

如果问题仍未解决，可以：
- 查看飞书开放平台文档：https://open.feishu.cn/document
- 联系飞书技术支持
