import requests
import json

# 读取配置文件获取访问凭证
config_path = "mcp-config.json"
with open(config_path, "r", encoding="utf-8") as f:
    config = json.load(f)

# 从配置中提取token
token = config["mcpServers"]["feishu-remote"]["headers"]["Authorization"].split(" ")[1]

# MCP服务URL
mcp_url = "https://mcp.feishu.cn/mcp"

# 构建请求头
headers = {
    "X-Lark-MCP-TAT": token,
    "Content-Type": "application/json",
    "X-Lark-MCP-Allowed-Tools": "fetch-doc,update-doc"
}

# 文档ID
doc_id = "EIWcd8E86oX8xDxWLBqcDzmZnQd"

# 构建获取文档的请求体
fetch_doc_payload = {
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/call",
    "params": {
        "name": "fetch-doc",
        "arguments": {
            "url": f"https://www.feishu.cn/docx/{doc_id}"
        }
    }
}

print("正在获取俄罗斯方块游戏需求文档...")

# 发送获取文档请求
response = requests.post(mcp_url, headers=headers, json=fetch_doc_payload)

print(f"响应状态码: {response.status_code}")

# 解析响应
try:
    result = response.json()
    print("获取文档成功！")
    
    # 提取文档内容
    if result.get("result") and result["result"].get("content"):
        content = result["result"]["content"]
        
        # 构建更新后的文档内容
        # 我们需要在产品结构和功能需求中添加灰度模块
        updated_markdown = """## 1. 变更记录
| 版本 | 日期 | 变更人 | 变更内容 |
|------|------|--------|----------|
| v1.0 | 2026-03-26 | 系统 | 初始化需求文档 |
| v1.1 | 2026-03-26 | 系统 | 添加灰度模块 |

## 2. 文档概述
本文档描述了俄罗斯方块游戏的功能需求和技术实现方案，包括游戏核心功能、用户界面、游戏规则等内容。

## 3. 项目背景
俄罗斯方块是一款经典的休闲益智游戏，具有广泛的用户基础和市场需求。本项目旨在开发一款现代化的俄罗斯方块游戏，提供良好的用户体验和游戏性。

## 4. 业务场景
- **休闲娱乐**：用户在碎片化时间进行游戏，放松身心
- **挑战自我**：用户通过不断挑战提高自己的游戏水平和分数
- **社交分享**：用户可以分享自己的游戏成绩和成就
- **灰度测试**：通过灰度测试验证游戏功能和用户体验

## 5. 目标用户
- **休闲游戏玩家**：喜欢简单易上手的游戏
- **怀旧游戏爱好者**：对经典俄罗斯方块游戏有情感连接
- **各年龄段用户**：游戏规则简单，适合不同年龄段的用户
- **灰度测试用户**：参与游戏灰度测试的用户

## 6. 产品结构
```mermaid
graph TD
    A[俄罗斯方块游戏] --> B[游戏核心模块]
    A --> C[游戏控制模块]
    A --> D[数据管理模块]
    A --> E[用户界面模块]
    A --> F[灰度模块]
    B --> G[方块生成]
    B --> H[碰撞检测]
    B --> I[行消除]
    B --> J[分数计算]
    C --> K[键盘控制]
    C --> L[游戏状态控制]
    D --> M[分数记录]
    D --> N[最高分存储]
    E --> O[游戏区域]
    E --> P[信息面板]
    E --> Q[控制按钮]
    F --> R[灰度用户管理]
    F --> S[灰度权限控制]
    F --> T[灰度数据收集]
```

## 7. 流程图
```mermaid
flowchart TD
    A[开始游戏] --> B{是否灰度用户?}
    B -->|否| C[显示灰度提示]
    C --> D[退出游戏]
    B -->|是| E{是否启用?}
    E -->|否| C
    E -->|是| F[初始化游戏]
    F --> G[生成方块]
    G --> H[移动方块]
    H --> I{碰撞检测}
    I -->|碰撞| J[固定方块]
    J --> K{是否满行}
    K -->|是| L[消除行]
    L --> M[计算分数]
    M --> N{游戏结束?}
    K -->|否| N
    N -->|否| G
    N -->|是| O[显示游戏结束]
    O --> P[记录分数]
    P --> Q[收集灰度数据]
    Q --> R[重新开始?]
    R -->|是| F
    R -->|否| S[退出游戏]
```

## 8. 高保真原型
### 8.1 游戏主界面
- **游戏区域**：10x20的网格，显示正在下落的方块和已固定的方块
- **信息面板**：显示当前分数、最高分、当前等级、下一个方块预览
- **控制按钮**：开始/暂停、重新开始、难度调整
- **操作说明**：显示键盘控制说明

### 8.2 游戏结束界面
- **游戏结束提示**：显示"游戏结束"字样
- **最终分数**：显示本局游戏的最终分数
- **重新开始按钮**：点击重新开始游戏
- **返回主菜单按钮**：点击返回主菜单

### 8.3 灰度提示界面
- **灰度提示**：显示"本游戏仅对灰度用户开放"字样
- **了解更多按钮**：点击了解灰度测试相关信息
- **返回按钮**：点击返回

## 9. 功能需求

### 9.1 游戏核心模块
| 功能点 | 描述 | 操作逻辑 | 状态流转 |
|--------|------|----------|----------|
| 方块生成 | 随机生成不同形状的方块 | 游戏开始时和每次方块固定后生成新方块 | 生成 → 下落 → 固定 |
| 方块移动 | 控制方块左右移动和加速下落 | 通过键盘方向键控制 | 左移 → 右移 → 下移 → 固定 |
| 方块旋转 | 旋转方块的方向 | 通过键盘上键控制 | 旋转 → 碰撞检测 → 固定 |
| 碰撞检测 | 检测方块是否与边界或其他方块碰撞 | 每次移动和旋转时进行检测 | 检测 → 碰撞 → 固定 |
| 行消除 | 当一行被方块填满时消除该行 | 固定方块后检查是否有满行 | 满行 → 消除 → 分数增加 |
| 分数计算 | 根据消除的行数计算分数 | 消除行后根据行数计算分数 | 消除行 → 计算分数 → 更新显示 |
| 游戏结束 | 当方块堆叠到顶部时游戏结束 | 固定方块后检查是否触顶 | 触顶 → 游戏结束 → 显示结果 |

### 9.2 游戏控制模块
| 功能点 | 描述 | 操作逻辑 | 状态流转 |
|--------|------|----------|----------|
| 开始游戏 | 开始新游戏 | 点击开始按钮或按空格键 | 准备 → 游戏中 |
| 暂停/继续 | 暂停或继续游戏 | 点击暂停按钮或按P键 | 游戏中 → 暂停 → 游戏中 |
| 重新开始 | 重新开始游戏 | 点击重新开始按钮或按R键 | 任意状态 → 准备 → 游戏中 |
| 难度调整 | 调整游戏速度 | 通过难度滑块或按钮调整 | 准备状态 → 调整难度 → 游戏中 |

### 9.3 数据管理模块
| 功能点 | 描述 | 操作逻辑 | 状态流转 |
|--------|------|----------|----------|
| 分数记录 | 记录当前游戏的分数 | 每次消除行后更新分数 | 初始分数 → 分数增加 → 最终分数 |
| 最高分存储 | 存储历史最高分数 | 游戏结束后如果分数高于历史最高则更新 | 历史最高分 → 比较 → 更新 |
| 游戏等级 | 根据分数提升游戏等级 | 达到一定分数后提升等级，增加下落速度 | 等级1 → 等级2 → ... → 等级N |

### 9.4 用户界面模块
| 功能点 | 描述 | 操作逻辑 | 状态流转 |
|--------|------|----------|----------|
| 游戏区域 | 显示游戏网格和方块 | 实时更新方块位置和状态 | 初始化 → 实时更新 → 游戏结束 |
| 信息面板 | 显示游戏信息 | 实时更新分数、等级等信息 | 初始化 → 实时更新 → 游戏结束 |
| 控制按钮 | 提供游戏控制功能 | 点击按钮触发相应操作 | 点击 → 执行操作 → 状态更新 |
| 响应式设计 | 适配不同屏幕尺寸 | 根据屏幕尺寸调整界面布局 | 检测屏幕尺寸 → 调整布局 |

### 9.5 灰度模块
| 功能点 | 描述 | 操作逻辑 | 状态流转 |
|--------|------|----------|----------|
| 灰度用户管理 | 管理灰度测试用户列表 | 后台配置灰度用户名单 | 配置 → 验证 → 访问 |
| 灰度用户启用/禁用 | 控制灰度用户的启用和禁用状态 | 后台管理界面中对灰度用户进行启用/禁用操作 | 启用 → 验证 → 访问 / 禁用 → 拒绝访问 |
| 灰度权限控制 | 控制灰度用户访问权限 | 游戏启动时验证用户是否为灰度用户且状态为启用 | 启动 → 验证 → 授权/拒绝 |
| 灰度数据收集 | 收集灰度测试数据 | 游戏过程中收集用户行为和游戏数据 | 游戏中 → 收集 → 分析 |
| 灰度提示 | 非灰度用户访问时显示提示 | 非灰度用户或被禁用的灰度用户尝试访问时显示灰度提示 | 非灰度用户/被禁用用户 → 显示提示 → 退出 |

## 10. 非功能需求
| 需求点 | 描述 | 验收标准 |
|--------|------|----------|
| 性能要求 | 游戏运行流畅，无卡顿 | 帧率稳定在60FPS以上 |
| 兼容性 | 支持主流浏览器 | 在Chrome、Firefox、Safari、Edge等浏览器中正常运行 |
| 响应时间 | 操作响应时间不超过100ms | 键盘操作后方块立即响应 |
| 安全性 | 无安全漏洞 | 代码通过安全扫描，无XSS等安全问题 |
| 可维护性 | 代码结构清晰，易于维护 | 代码遵循规范，有完善的注释 |
| 灰度控制 | 严格控制灰度用户访问 | 非灰度用户无法访问游戏 |

## 11. 项目范围
| 范围 | 包含 | 不包含 |
|------|------|--------|
| 核心功能 | 方块生成、移动、旋转、碰撞检测、行消除、分数计算、游戏结束 | 在线 multiplayer、复杂的视觉效果 |
| 用户界面 | 游戏主界面、游戏结束界面、灰度提示界面、响应式设计 | 3D效果、复杂动画 |
| 数据管理 | 分数记录、最高分存储、游戏等级、灰度数据收集 | 在线排行榜、成就系统 |
| 控制方式 | 键盘控制、鼠标点击 | 触摸屏控制、语音控制 |
| 灰度功能 | 灰度用户管理、灰度用户启用/禁用、灰度权限控制、灰度数据收集 | 灰度用户邀请、灰度测试奖励 |

## 12. 风险分析
| 风险点 | 影响 | 应对措施 |
|--------|------|----------|
| 浏览器兼容性问题 | 部分浏览器可能不支持某些特性 | 使用兼容性更好的技术，进行多浏览器测试 |
| 性能优化 | 游戏运行不流畅 | 优化代码，减少不必要的计算，使用requestAnimationFrame |
| 存储安全 | 最高分存储可能被篡改 | 使用localStorage存储，定期备份 |
| 用户体验 | 操作不直观 | 提供清晰的操作说明，优化控制方式 |
| 灰度控制 | 灰度用户验证失败 | 实现可靠的用户验证机制，提供错误处理 |

## 13. 验收标准
| 功能点 | 验收标准 |
|--------|----------|
| 游戏核心功能 | 方块能正确生成、移动、旋转、碰撞检测、行消除、分数计算 |
| 游戏控制 | 开始、暂停、重新开始等操作正常响应 |
| 数据管理 | 分数记录准确，最高分正确存储和更新 |
| 用户界面 | 界面美观，响应式设计，在不同屏幕尺寸下正常显示 |
| 灰度功能 | 灰度用户可以正常访问游戏，非灰度用户显示灰度提示 |
| 性能 | 游戏运行流畅，无卡顿 |
| 兼容性 | 在主流浏览器中正常运行 |

## 14. 交付物
| 交付物 | 描述 |
|--------|------|
| 游戏代码 | 完整的俄罗斯方块游戏代码 |
| 需求文档 | 本需求文档 |
| 测试报告 | 游戏测试结果报告 |
| 部署指南 | 游戏部署和运行指南 |
| 灰度测试方案 | 灰度测试计划和执行方案 |

## 15. 项目时间计划
| 阶段 | 时间 | 任务 |
|------|------|------|
| 需求分析 | 1天 | 分析需求，编写需求文档 |
| 设计阶段 | 2天 | 游戏设计，界面设计，灰度功能设计 |
| 开发阶段 | 5天 | 游戏核心功能开发，界面开发，灰度功能开发 |
| 测试阶段 | 2天 | 功能测试，性能测试，兼容性测试，灰度测试 |
| 部署阶段 | 1天 | 游戏部署，灰度配置，上线 |

## 16. 联系方式
| 角色 | 姓名 | 邮箱 | 电话 |
|------|------|------|------|
| 产品经理 | 张三 | zhangsan@example.com | 13800138000 |
| 开发工程师 | 李四 | lisi@example.com | 13900139000 |
| 测试工程师 | 王五 | wangwu@example.com | 13700137000 |
| 灰度测试负责人 | 赵六 | zhaoliu@example.com | 13600136000 |
"""

        # 构建更新文档的请求体
        update_doc_payload = {
            "jsonrpc": "2.0",
            "id": 2,
            "method": "tools/call",
            "params": {
                "name": "update-doc",
                "arguments": {
                    "doc_id": doc_id,
                    "mode": "overwrite",
                    "markdown": updated_markdown
                }
            }
        }

        print("\n正在更新俄罗斯方块游戏需求文档，添加灰度模块...")

        # 发送更新文档请求
        update_response = requests.post(mcp_url, headers=headers, json=update_doc_payload)

        print(f"响应状态码: {update_response.status_code}")
        print(f"响应内容: {update_response.text}")

        try:
            update_result = update_response.json()
            print("\n文档更新成功！")
            print(f"文档URL: https://www.feishu.cn/docx/{doc_id}")
        except json.JSONDecodeError as e:
            print(f"JSON解析失败: {e}")
        except Exception as e:
            print(f"处理响应失败: {e}")

except json.JSONDecodeError as e:
    print(f"JSON解析失败: {e}")
except Exception as e:
    print(f"处理响应失败: {e}")
