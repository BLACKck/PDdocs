# 宠物服务系统流程图

## 1. 系统架构图

```mermaid
graph TD
    subgraph 客户端
        A[对客前端] -->|HTTP请求| B[API网关]
        C[店端管理系统] -->|HTTP请求| B
    end
    
    subgraph 服务端
        B -->|路由请求| D[对客服务]
        B -->|路由请求| E[店端服务]
        D -->|调用| F[业务逻辑层]
        E -->|调用| F
        F -->|操作| G[数据库]
        F -->|调用| H[第三方服务]
    end
    
    subgraph 外部服务
        H -->|支付| I[支付平台]
        H -->|存储| J[云存储]
        H -->|推送| K[消息推送]
    end
```

## 2. 对客前端流程

### 2.1 用户注册/登录流程

```mermaid
sequenceDiagram
    participant 用户
    participant 前端
    participant 后端
    participant 数据库
    
    用户->>前端: 输入注册信息
    前端->>后端: 发送注册请求
    后端->>数据库: 检查用户是否存在
    alt 用户已存在
        数据库-->>后端: 返回用户存在
        后端-->>前端: 返回注册失败
        前端-->>用户: 显示注册失败
    else 用户不存在
        数据库-->>后端: 返回用户不存在
        后端->>数据库: 创建新用户
        数据库-->>后端: 返回创建成功
        后端-->>前端: 返回注册成功
        前端-->>用户: 显示注册成功
    end
    
    用户->>前端: 输入登录信息
    前端->>后端: 发送登录请求
    后端->>数据库: 验证用户信息
    alt 验证成功
        数据库-->>后端: 返回用户信息
        后端->>后端: 生成Token
        后端-->>前端: 返回登录成功和Token
        前端-->>用户: 显示登录成功
    else 验证失败
        数据库-->>后端: 返回验证失败
        后端-->>前端: 返回登录失败
        前端-->>用户: 显示登录失败
    end
```

### 2.2 服务预约流程

```mermaid
sequenceDiagram
    participant 用户
    participant 前端
    participant 后端
    participant 数据库
    
    用户->>前端: 选择服务类型
    前端->>后端: 请求可预约时间
    后端->>数据库: 查询可用时间
    数据库-->>后端: 返回可用时间
    后端-->>前端: 返回可预约时间
    前端-->>用户: 显示可预约时间
    
    用户->>前端: 选择预约时间
    前端->>后端: 发送预约请求
    后端->>数据库: 创建预约记录
    数据库-->>后端: 返回创建成功
    后端-->>前端: 返回预约成功
    前端-->>用户: 显示预约成功
```

### 2.3 宠物圈发帖流程

```mermaid
sequenceDiagram
    participant 用户
    participant 前端
    participant 后端
    participant 数据库
    participant 云存储
    
    用户->>前端: 输入帖子内容
    用户->>前端: 上传图片/视频
    前端->>云存储: 上传媒体文件
    云存储-->>前端: 返回文件URL
    前端->>后端: 发送发帖请求
    后端->>数据库: 创建帖子记录
    数据库-->>后端: 返回创建成功
    后端-->>前端: 返回发帖成功
    前端-->>用户: 显示发帖成功
```

### 2.4 商品购买流程

```mermaid
sequenceDiagram
    participant 用户
    participant 前端
    participant 后端
    participant 数据库
    participant 支付平台
    
    用户->>前端: 浏览商品
    前端->>后端: 请求商品列表
    后端->>数据库: 查询商品信息
    数据库-->>后端: 返回商品列表
    后端-->>前端: 返回商品列表
    前端-->>用户: 显示商品列表
    
    用户->>前端: 选择商品加入购物车
    前端->>后端: 发送加入购物车请求
    后端->>数据库: 更新购物车
    数据库-->>后端: 返回更新成功
    后端-->>前端: 返回加入成功
    
    用户->>前端: 确认订单
    前端->>后端: 发送订单请求
    后端->>数据库: 创建订单
    数据库-->>后端: 返回订单信息
    后端->>支付平台: 发起支付
    支付平台-->>后端: 返回支付链接
    后端-->>前端: 返回支付链接
    前端-->>用户: 跳转到支付页面
    
    用户->>支付平台: 完成支付
    支付平台-->>后端: 支付结果通知
    后端->>数据库: 更新订单状态
    数据库-->>后端: 返回更新成功
    后端-->>前端: 通知支付成功
    前端-->>用户: 显示支付成功
```

## 3. 店端管理系统流程

### 3.1 商品管理流程

```mermaid
sequenceDiagram
    participant 管理员
    participant 前端
    participant 后端
    participant 数据库
    
    管理员->>前端: 登录系统
    前端->>后端: 发送登录请求
    后端->>数据库: 验证管理员信息
    数据库-->>后端: 返回验证成功
    后端-->>前端: 返回登录成功
    
    管理员->>前端: 进入商品管理
    前端->>后端: 请求商品列表
    后端->>数据库: 查询商品信息
    数据库-->>后端: 返回商品列表
    后端-->>前端: 返回商品列表
    前端-->>管理员: 显示商品列表
    
    管理员->>前端: 添加/编辑商品
    前端->>后端: 发送商品信息
    后端->>数据库: 保存商品信息
    数据库-->>后端: 返回保存成功
    后端-->>前端: 返回操作成功
    前端-->>管理员: 显示操作成功
```

### 3.2 订单处理流程

```mermaid
sequenceDiagram
    participant 管理员
    participant 前端
    participant 后端
    participant 数据库
    
    管理员->>前端: 进入订单管理
    前端->>后端: 请求订单列表
    后端->>数据库: 查询订单信息
    数据库-->>后端: 返回订单列表
    后端-->>前端: 返回订单列表
    前端-->>管理员: 显示订单列表
    
    管理员->>前端: 选择订单处理
    前端->>后端: 发送订单处理请求
    后端->>数据库: 更新订单状态
    数据库-->>后端: 返回更新成功
    后端-->>前端: 返回处理成功
    前端-->>管理员: 显示处理成功
```

### 3.3 宠物信息管理流程

```mermaid
sequenceDiagram
    participant 管理员
    participant 前端
    participant 后端
    participant 数据库
    
    管理员->>前端: 进入宠物信息管理
    前端->>后端: 请求宠物列表
    后端->>数据库: 查询宠物信息
    数据库-->>后端: 返回宠物列表
    后端-->>前端: 返回宠物列表
    前端-->>管理员: 显示宠物列表
    
    管理员->>前端: 查看宠物详情
    前端->>后端: 请求宠物详情
    后端->>数据库: 查询宠物详细信息
    数据库-->>后端: 返回宠物详情
    后端-->>前端: 返回宠物详情
    前端-->>管理员: 显示宠物详情
    
    管理员->>前端: 添加/编辑宠物信息
    前端->>后端: 发送宠物信息
    后端->>数据库: 保存宠物信息
    数据库-->>后端: 返回保存成功
    后端-->>前端: 返回操作成功
    前端-->>管理员: 显示操作成功
```

### 3.4 帖子管理流程

```mermaid
sequenceDiagram
    participant 管理员
    participant 前端
    participant 后端
    participant 数据库
    
    管理员->>前端: 进入帖子管理
    前端->>后端: 请求帖子列表
    后端->>数据库: 查询帖子信息
    数据库-->>后端: 返回帖子列表
    后端-->>前端: 返回帖子列表
    前端-->>管理员: 显示帖子列表
    
    管理员->>前端: 审核帖子
    前端->>后端: 发送审核结果
    后端->>数据库: 更新帖子状态
    数据库-->>后端: 返回更新成功
    后端-->>前端: 返回审核成功
    前端-->>管理员: 显示审核成功
```

## 4. 数据流程图

```mermaid
graph TD
    A[用户] -->|注册/登录| B[用户表]
    A -->|添加宠物| C[宠物表]
    A -->|预约服务| D[预约表]
    A -->|发布帖子| E[帖子表]
    A -->|评论| F[评论表]
    A -->|购买商品| G[订单表]
    
    H[管理员] -->|管理用户| B
    H -->|管理宠物| C
    H -->|管理预约| D
    H -->|管理帖子| E
    H -->|管理评论| F
    H -->|管理订单| G
    H -->|管理商品| I[商品表]
    H -->|管理库存| J[库存表]
    
    G -->|关联| I
    G -->|关联| B
    D -->|关联| C
    D -->|关联| B
    E -->|关联| B
    F -->|关联| E
    F -->|关联| B
```