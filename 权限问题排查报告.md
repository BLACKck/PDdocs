# 权限问题排查报告

## 问题现象

使用 User Access Token 调用飞书云文档创建 API 时返回错误：
```
错误码：99991679
错误信息：Unauthorized. You do not have permission to perform the requested operation on the resource. 
required one of these privileges under the user identity: [docx:document, docx:document:create]
```

## 当前状态

### ✓ 已验证
- User Access Token 有效
- 用户信息可以正常获取
- 已开通 `docx:document:create` 权限（用户身份）

### ✗ 问题
- 缺少 `docx:document` 权限
- 或者权限未生效

## 需要的权限

创建云文档需要同时具备以下两个权限：

| 权限名称 | 权限代码 | 权限类型 | 状态 |
|---------|---------|---------|------|
| 查看和编辑新版文档 | `docx:document` | 用户身份 | ❓ 待确认 |
| 创建新版文档 | `docx:document:create` | 用户身份 | ✅ 已开通 |

## 解决方案

### 方案 1：开通 docx:document 权限（推荐）

1. **访问飞书开放平台**
   - URL: https://open.feishu.cn/
   - 登录开发者账号

2. **进入应用管理**
   - 点击"开发者后台"
   - 选择应用（俄罗斯方块，App ID: cli_a92fb9e90e799cb5）

3. **开通权限**
   - 点击左侧菜单"权限管理"
   - 点击"开通权限"按钮
   - 搜索 `docx:document`
   - 选择"查看和编辑新版文档"权限
   - 权限类型选择"**用户身份**"
   - 数据范围选择"与用户权限范围一致"
   - 点击"保存"

4. **提交审核**
   - 如果权限需要审核，提交审核申请
   - 填写使用说明
   - 等待审核通过（通常 1-2 个工作日）

5. **重新授权**
   - 审核通过后
   - 在飞书开放平台 > 应用管理 > 版本管理中重新发布应用
   - 或者让用户重新扫码授权

### 方案 2：检查现有权限配置

1. **检查权限类型**
   - 进入飞书开放平台 > 应用管理 > 权限管理
   - 找到 `docx:document:create` 权限
   - 确认权限类型是"用户身份"（不是"应用身份"）
   - 确认权限状态是"已开通"（不是"审核中"）

2. **检查数据范围**
   - 点击权限的"配置"按钮
   - 确认数据范围设置正确
   - 建议选择"与用户权限范围一致"

3. **重新保存权限**
   - 编辑现有权限配置
   - 重新保存
   - 等待配置生效（可能需要几分钟）

### 方案 3：重新授权应用

1. **在飞书开放平台操作**
   - 进入应用管理 > 选择应用
   - 点击"版本管理"
   - 点击"重新发布"或"更新版本"

2. **用户重新授权**
   - 使用飞书 App 扫码
   - 进入应用授权页面
   - 点击"重新授权"
   - 确认授权所有权限

3. **获取新的 User Access Token**
   - 重新运行 OAuth 授权流程
   - 或者使用 refresh_token 刷新
   - 运行：`python reauth_get_token.py`

## 快速验证

权限配置完成后，运行以下命令验证：

```bash
# 1. 检查用户信息（验证 token 有效性）
python check_user_permissions.py

# 2. 尝试创建文档
python create_docx_api.py
```

如果看到以下输出，说明权限配置成功：
```
✓ 云文档创建成功！
文档 ID: xxxxxxxxxxxxxx
文档 URL: https://xxx.feishu.cn/docx/xxxxxxxxxxxxxx
```

## 权限代码说明

飞书云文档相关权限代码：

| 权限代码 | 权限名称 | 说明 |
|---------|---------|------|
| `docx:document` | 查看和编辑新版文档 | 基础权限，必须开通 |
| `docx:document:create` | 创建新版文档 | 创建文档必须 |
| `docx:document:read` | 查看新版文档 | 读取文档内容 |
| `docx:document:edit` | 编辑新版文档 | 编辑文档内容 |
| `docx:document:delete` | 删除新版文档 | 删除文档 |
| `docx:document:readonly` | 只读访问 | 仅查看，不能编辑 |

**注意**：`docx:document:readonly` 不等于 `docx:document`

## 常见错误

### 错误 1：权限类型错误
- **现象**：开通了权限但仍然报错
- **原因**：开通的是"应用身份"权限，不是"用户身份"权限
- **解决**：重新开通"用户身份"类型的权限

### 错误 2：权限不完整
- **现象**：报错提示需要多个权限
- **原因**：只开通了部分权限
- **解决**：开通所有提示需要的权限

### 错误 3：Token 过期
- **现象**：之前可以，突然不行了
- **原因**：User Access Token 过期
- **解决**：重新获取 User Access Token

### 错误 4：权限未生效
- **现象**：开通了权限但还是报错
- **原因**：权限配置需要时间生效，或者需要重新授权
- **解决**：等待几分钟后重试，或者重新授权

## 联系支持

如果以上方案都无法解决问题，可以：

1. **查看官方文档**
   - 飞书开放平台：https://open.feishu.cn/document
   - 云文档 API 文档：https://open.feishu.cn/document/ugTN1YjL4UTN24CO1UjN

2. **查看错误日志**
   - 使用错误信息中的 log_id 查询详细错误
   - 例如：log_id=202603262254297C8C79AAA9886C455D96

3. **联系飞书技术支持**
   - 通过飞书开放平台提交工单
   - 提供错误日志和 app_id

## 下一步行动

1. ✅ 检查是否开通了 `docx:document` 权限
2. ✅ 确认权限类型是"用户身份"
3. ✅ 确认权限状态是"已开通"
4. ⏳ 如果未开通，立即开通
5. ⏳ 开通后重新授权应用
6. ⏳ 重新获取 User Access Token
7. ⏳ 运行测试脚本验证

## 测试脚本清单

| 脚本名称 | 用途 | 命令 |
|---------|------|------|
| check_user_permissions.py | 检查用户权限 | `python check_user_permissions.py` |
| create_docx_api.py | 创建云文档 | `python create_docx_api.py` |
| reauth_get_token.py | 重新获取 token | `python reauth_get_token.py` |
| refresh_token.py | 刷新 token | `python refresh_token.py` |

---

**创建时间**: 2026-03-26  
**最后更新**: 2026-03-26  
**状态**: 待解决
