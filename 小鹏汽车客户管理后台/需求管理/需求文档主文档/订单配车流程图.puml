@startuml 订单配车流程图

start

:配车规则设置;
:订单审批通过;
:开始配车;

if (配车方式?)
  then (智能配车)
    :系统自动匹配车辆;
  else (手动配车)
    :人工选择车辆;
  endif

if (配车成功?)
  then (是)
    :配车结果确认;
    :更新订单状态为已配车;
  else (否)
    :配车失败;
    :记录失败原因;
    :重新配车;
    back to :开始配车;
  endif

stop

@enduml